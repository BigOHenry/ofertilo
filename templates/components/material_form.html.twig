{{ form_start(form, {
  attr: {
    id: form_id|default(null),
    class: 'needs-validation',
  }
}) }}

  {{ form_errors(form) }}

  {{ form_row(form.wood) }}
  {{ form_row(form.type) }}

  {{ form_row(form.save) }}
  <input type="hidden" name="frame_id" value="{{ frame_id }}">

{{ form_end(form) }}

